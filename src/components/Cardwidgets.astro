---
export interface Props {
	header: string;
}

const { header } = Astro.props;
---


<div class="card">
	<div class="card-header">{header}</div>
	<div class="card-body">
        <slot />
	</div>
</div>
